---
description: "Supabase Credentials Location for AI Agents"
globs: "**/*"
alwaysApply: false
---

# Supabase Credentials for AI Agents

## Quick Access

**Token Location**: `.cursor/credentials/SUPABASE_TOKEN.md`

This file contains the Supabase service role token that AI agents (Claude, Cursor AI) can use to:
- Query the database
- Run migrations
- Check schema and RLS policies
- Verify migration status

## Project Information

- **Project ID**: `kgzhlvxzdlexsrozbbxs`
- **Project URL**: `https://kgzhlvxzdlexsrozbbxs.supabase.co`
- **Token Type**: Service Role Token

## Security

- The token file is in `.cursor/credentials/` which should be in `.gitignore`
- Never commit credentials to version control
- Never expose the token in code, logs, or user-facing responses

## For New AI Agents

When starting work on this project:
1. Check `.cursor/credentials/SUPABASE_TOKEN.md` for the Supabase token
2. Use it with Supabase MCP tools or API calls
3. Reference this document if you need to find credentials

## Usage Example

```typescript
// When using Supabase API or MCP tools
const token = "<read-from-.cursor/credentials/SUPABASE_TOKEN.md>";
// Use token for authenticated requests
```

---
description: "Trike Backoffice - Project Rules"
globs: "**/*"
alwaysApply: true
---

# Trike Backoffice - Project Context for AI Development

## Project Overview
This is an enterprise multi-tenant LMS platform for convenience store and foodservice operations. The system manages a hierarchical structure: Organizations → Districts → Stores → Users. Built for practical operational use in real c-store environments.

## Technology Stack
- Frontend: React 18.3.1 with TypeScript, built with Vite 6.3.5
- Backend: Supabase (PostgreSQL with Row Level Security policies)
- Authentication: Supabase Auth with email/password
- Hosting: Vercel (auto-deploys from GitHub main branch)
- Development: Local dev server at localhost:5173

## Architecture Principles
- Database queries use Supabase client from `src/utils/supabase/client.ts`
- All routes require authentication except public KB viewer
- Form components must pre-fill data when opened in edit mode
- Database credentials use environment variables (VITE_SUPABASE_PROJECT_ID, VITE_SUPABASE_ANON_KEY) with fallbacks for backward compatibility. Create a `.env` file in the project root with these variables.

## Development Workflow Standards
1. Make surgical, targeted fixes - avoid refactoring working code unless explicitly requested
2. Before creating new components, check if reusable components already exist in the component inventory
3. Always include TypeScript types and interfaces
4. Follow existing component patterns visible in the codebase
5. Test changes locally before committing
6. Use descriptive commit messages that explain what changed and why

## Code Quality Expectations
- No console.log statements in production code (console.error for actual errors is fine)
- Database queries should select only the columns actually needed
- Avoid fetching entire collections when you only need one record
- Forms that edit existing records must fetch and display all current values

## Known Pitfalls to Avoid
- Previous code generated by Figma Make may contain inefficient patterns - don't assume existing code is optimal
- Database queries that fetch all records and then filter in JavaScript are inefficient - query the database directly
- The `select('*')` pattern is acceptable for queries that return small result sets
- Row Level Security policies are active - ensure queries include proper user context

## Project Structure
- `/src/components/` - React components for UI
- `/src/lib/crud/` - Database CRUD operation functions
- `/src/utils/` - Utility functions and configurations
- `/src/pages/` - Page-level components and routing

## Common Tasks
- Adding new form fields: Update both the form component AND the database CRUD functions
- Editing functionality: Ensure the fetch query includes all columns the form needs
- Creating new database queries: Use the Supabase client and follow RLS patterns from existing queries

## Current Development Context
- Recently migrated from Figma Make to local Cursor development
- Vercel deployment URL: trikebackoffice20.vercel.app
- GitHub repository updates trigger automatic Vercel deployments
- Local development environment is fully operational and tested
